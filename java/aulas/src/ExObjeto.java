public class ExObjeto {
	                   
	double p, ini, x, pInicial;
	int qntdMoedas;
	
	
	
	/*
	 * p = preço da moeda
	 * ini = investimento inicial
	 * qntdMoedas = número de moedas = investimento inicial (ini)/ preço da moeda(p)
	 * x = porcentagem de aumento do preço
		reçoIni = preço inicial da moeda
	 * 
	 *  valorVenda = (0.2 * qntdMoedas) * (p + (p * x))
	 qntdMoedas = qntdMoedas - (0.2 * qntdMoedas)
	 p = p + (p * x)
	 *  valorVenda = (0.2 * qntdMoedas) * (p + (p * x))
 
	 valorVenda - valorCompra = lucro
	 (0.2 * (ini / p)) * (p + (p * x)) - (0.2 * (ini / preçoIni)) > 0

	p = 5
	ini = 100
	qntdMoedas = ini / p = 20
	x = ?

	(0.2 * 20) * (5 + (5 * x)) - (0.2 * 20) > 0
	4 * (5 + 5x) - 4 > 0
	20 + 20x - 4 > 0
	20x > - 16
	x > 
	 */ 
	
}
